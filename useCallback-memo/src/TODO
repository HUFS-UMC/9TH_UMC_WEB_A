TODO
1.디팬던시 배열(의존성 배열)을 신중하게 설정
-만약 count 넣지 않으면 최신값이 반영되지 않을수 있었다.
- 너무 많은 변수를 넣은 경우는, 최적화 효과가 사라질 수 있다.

2useCallback TradeOff
-useCallback 함수가 메모리에 저장되기 떄문에, 메모리에 많이 사용
-정말 필요할때사용하자.


memo 언제 사용?
-> props가 변경되지 않으면 다시 렌더링되고 싶지않을떄 ,
근대 참조값은 false왜냐면 함수가 동일해 보여도 참조값이 다르면
컴포넌트를 memorization하는 조건에 해당하지 않는다. 그래서 react.memo작동x
그래서 함수의 참조값을 유지해서 불필요한 재생성을 유지하는 usecallback사용
memo->컴포넌트를 캐싱
useCallback->함수를캐싱